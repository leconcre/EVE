# ═══════════════════════════════════════════════════════════════════
# DEPENDÊNCIAS DO MÓDULO DE VOZ DA EVE
# ═══════════════════════════════════════════════════════════════════
#
# Instalar tudo:
#   pip install -r voice/requirements.txt
#
# Instalação mínima (apenas o essencial):
#   pip install -r voice/requirements-minimal.txt
#
# ═══════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────
# CORE - Essenciais
# ───────────────────────────────────────────────────────────────────

numpy>=1.21.0
scipy>=1.7.0

# ───────────────────────────────────────────────────────────────────
# ÁUDIO - Captura e reprodução
# ───────────────────────────────────────────────────────────────────

# PyAudio - Captura de microfone
# NOTA: No Windows, pode ser necessário baixar wheel pré-compilado:
# https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
pyaudio>=0.2.11

# SoundDevice - Reprodução de áudio
sounddevice>=0.4.5

# ───────────────────────────────────────────────────────────────────
# SPEECH-TO-TEXT - Whisper
# ───────────────────────────────────────────────────────────────────

# faster-whisper (RECOMENDADO - otimizado e rápido)
faster-whisper>=0.9.0

# OU openai-whisper (original, mais pesado)
# openai-whisper>=20230918

# ───────────────────────────────────────────────────────────────────
# TEXT-TO-SPEECH - Múltiplas engines
# ───────────────────────────────────────────────────────────────────

# Edge TTS - Microsoft (RECOMENDADO - excelente qualidade, fácil instalação)
edge-tts>=6.1.0

# pyttsx3 - Offline, básico (alternativa offline simples)
pyttsx3>=2.90

# gTTS - Google TTS (online, simples)
gTTS>=2.3.0

# Coqui TTS - Melhor qualidade (OPCIONAL - pode dar erro no Windows)
# ATENÇÃO: Se der erro ao instalar, veja voice/INSTALL_TTS.md
# Comentado por padrão, descomente apenas se conseguir instalar:
# TTS>=0.20.0

# ───────────────────────────────────────────────────────────────────
# VAD - Voice Activity Detection
# ───────────────────────────────────────────────────────────────────

# PyTorch - Para Silero VAD (melhor qualidade)
torch>=2.0.0
torchaudio>=2.0.0

# WebRTC VAD - Alternativa leve
webrtcvad>=2.0.10

# ───────────────────────────────────────────────────────────────────
# PROCESSAMENTO DE ÁUDIO
# ───────────────────────────────────────────────────────────────────

# Redução de ruído (opcional, mas recomendado)
noisereduce>=2.0.0

# ───────────────────────────────────────────────────────────────────
# DISCORD - Integração (opcional)
# ───────────────────────────────────────────────────────────────────

# discord.py com suporte a voz
discord.py[voice]>=2.0.0

# PyNaCl - Requerido para voz no Discord
PyNaCl>=1.5.0

# ───────────────────────────────────────────────────────────────────
# UTILITÁRIOS
# ───────────────────────────────────────────────────────────────────

# python-dotenv - Para variáveis de ambiente
python-dotenv>=1.0.0

# ═══════════════════════════════════════════════════════════════════
# NOTAS DE INSTALAÇÃO
# ═══════════════════════════════════════════════════════════════════
#
# 1. FFmpeg é NECESSÁRIO mas não está no pip:
#    - Windows: winget install FFmpeg
#    - Linux: sudo apt install ffmpeg
#    - macOS: brew install ffmpeg
#
# 2. Piper TTS (opcional, melhor qualidade):
#    - Windows: winget install rhasspy.piper
#    - Linux/macOS: pip install piper-tts
#
# 3. PyAudio no Windows:
#    Se der erro, baixe o wheel pré-compilado:
#    https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
#    pip install PyAudio-0.2.11-cp39-cp39-win_amd64.whl
#
# 4. PyTorch:
#    Para CUDA (GPU):
#    pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 5. Para economizar espaço, você pode instalar apenas o essencial
#    e adicionar componentes conforme necessário.
#
# ═══════════════════════════════════════════════════════════════════
